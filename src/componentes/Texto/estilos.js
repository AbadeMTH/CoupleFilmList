import { StyleSheet } from "react-native";

export const estilos = StyleSheet.create({
    texto: {
        fontFamily: 'ArvoRegular',
        fontWeight: 'normal'
    },
    textoNegrito: {
        fontFamily: 'ArvoBold',
        fontWeight: 'normal'
    },
});